RCMC SecureOps database artifacts.

Source of truth for the schema is:

  backend/prisma/schema.prisma

Use Prisma migrations from the backend package:

  cd backend
  npm run prisma:migrate
  npm run prisma:seed
